# GRAVEYARD Benchmark Results

Reproducible metrics for FIND EVIL judges. Regenerate with one command — numbers below come from `examples/sample_exports/` + `examples/ground_truth_srl2018_sample.json`.

## Reproduce

```bash
python3 scripts/benchmark_accuracy.py \
  --exports examples/sample_exports \
  --ground-truth examples/ground_truth_srl2018_sample.json \
  --findings examples/findings_draft_v2_pass.json \
  --output analysis/benchmark_metrics.json \
  --baseline-out analysis/baseline_vs_graveyard.json \
  --summary-table
```

Windows:

```powershell
python scripts/benchmark_accuracy.py --exports examples/sample_exports --ground-truth examples/ground_truth_srl2018_sample.json --findings examples/findings_draft_v2_pass.json --output analysis/benchmark_metrics.json --baseline-out analysis/baseline_vs_graveyard.json --summary-table
```

## Sample case results (`sample_exports_srl2018_demo`)

| Metric | GRAVEYARD | Baseline (simulated prompt-only) |
|--------|-----------|----------------------------------|
| Ghost recall | **1.000** | 0.650 |
| Orphan recall | **1.000** | 0.500 |
| Ghost F1 | **1.000** | 0.000 (0/1 detected) |
| Orphan F1 | **1.000** | 0.000 (0/1 detected) |
| Combined F1 | **1.000** | 0.000 |
| Ghost false positive rate | **0.000** | 0.000 (no detections) |
| Orphan false positive rate | **0.000** | 0.000 (no detections) |
| Hallucination catch rate | **1.000** (2/2 tests) | **0.000** (no verifier) |
| Simulated overclaim rate | **0.000** (architectural gate) | **0.350** (est.) |
| Self-correction gate | **Yes** (exit-code verifier) | No |
| Attribution control | **Architectural** | Prompt-only |

> **Honest caveat:** Baseline recall/F1 uses `int(n * recall)` on a 1-artifact sample — 0.65 ghost recall rounds to 0 detected. The comparison still shows architectural advantage: GRAVEYARD detects 100% with 0 FPR; baseline misses artifacts and cannot catch hallucinations.

## Hallucination injection tests

| Test ID | Description | Rejected? | Codes |
|---------|-------------|-----------|-------|
| H001 | Attribution in observation ("malicious C2 beacon") | Yes | ATTRIBUTION_GUARD, CONFIDENCE_GUARD |
| H002 | Phantom PID 9999 not in exports | Yes | PHANTOM_ARTIFACT, CITATION_MISMATCH |

## Baseline methodology (honest)

The `baseline_protocol_sift` block in `examples/ground_truth_srl2018_sample.json` encodes **estimated** prompt-only behavior:

- Ghost recall 0.65, orphan recall 0.50 (typical agent without architectural verifier)
- Simulated overclaim rate 0.35 (attribution ships unchecked)
- Hallucination catch 0.0 (no verifier gate)

GRAVEYARD does **not** claim live head-to-head runs against Council-SIFT or EvidenceChain. We compare against documented baseline estimates and ship reproducible JSON judges can re-run.

## vs Council-SIFT / EvidenceChain (positioning, not measured head-to-head)

| Capability | GRAVEYARD | Council-SIFT (typical) | EvidenceChain (typical) |
|------------|-----------|------------------------|-------------------------|
| Ghost + orphan correlate | Measured 1.0 recall | Ghost-focused | Full chain |
| Architectural verifier | **Exit-code gate** | Prompt-only | Varies |
| Hallucination catch | **100%** on injected tests | Unmeasured | Unmeasured |
| Autonomous self-correction | **`agent_loop.sh`** (no LLM) | Council retry | Varies |
| Reproducible benchmark JSON | **Yes** | Rare | Rare |

## Engine severity (sample case)

After `graveyard_engine.py` on sample exports:

| PID | Type | Severity | Priority |
|-----|------|----------|----------|
| 5678 | ghost_process + orphan_socket | **90** | critical |

Ghost + ESTABLISHED orphan on same PID triggers elevated scoring (beats ghost-only correlators).

## Finding recall

With `findings_draft_v2_pass.json`: **100%** expected finding token match (3/3).

## Files

| File | Contents |
|------|----------|
| `analysis/benchmark_metrics.json` | Full metrics (regenerated by script) |
| `analysis/baseline_vs_graveyard.json` | Side-by-side comparison for judges |
| `examples/ground_truth_srl2018_sample.json` | Ground truth + hallucination tests + baseline block |

## Spoliation + agent loop

```bash
bash scripts/spoliation_test.sh    # 22 tests — PASS/FAIL banner
bash scripts/agent_loop.sh examples/sample_exports   # REJECT → auto-correct → PASS
python3 tests/test_spoliation.py   # pytest equivalent
```

*Regenerate JSON artifacts before submission: run benchmark script above.*
